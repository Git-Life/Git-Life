var secret  = require('./tempsecret');
var request = require('request');
var async   = require('async');

module.exports = {

  getOrgs: function (req, res) {

    
    // var query = req.query.searchTerm;
    // request({
    //   uri: root + 'search/'
    // })
  }
  
}